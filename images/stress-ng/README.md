Stress-NG Docker file
===================

stress-ng cli image

Is a scratch image containing stress-ng only

Build
-----
Execute to build `0.11.02` and **latest** version:  
```bash
dockerRepo=docker.io

./build.sh ${dockerRepo}
```
